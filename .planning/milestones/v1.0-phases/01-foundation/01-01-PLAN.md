---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - astro.config.mjs
  - package.json
  - tsconfig.json
  - .github/workflows/deploy.yml
  - public/favicon.svg
  - public/robots.txt
  - src/env.d.ts
autonomous: true
requirements:
  - FOUND-01

must_haves:
  truths:
    - "Pushing to main triggers a GitHub Actions workflow that builds and deploys the site"
    - "The live site is accessible at https://prashantkhanchandani.github.io with no manual steps"
    - "The build completes without errors locally (npm run build exits 0)"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "CI/CD pipeline — build and deploy to GitHub Pages on push to main"
      contains: "withastro/action@v5"
    - path: "astro.config.mjs"
      provides: "Astro configuration — site URL, static output, sitemap integration"
      contains: "prashantkhanchandani.github.io"
    - path: "package.json"
      provides: "Project manifest with Astro 5 and sitemap dependencies"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "GitHub Pages"
      via: "actions/deploy-pages@v4 using OIDC token"
      pattern: "id-token: write"
    - from: "astro.config.mjs"
      to: "build output"
      via: "output: 'static' — no server adapter"
      pattern: "output.*static"
---

<objective>
Scaffold an Astro 5 project and establish the complete GitHub Pages delivery pipeline. A placeholder index page is deployed and live before any content or styling work begins.

Purpose: Validate the entire deploy pipeline first — catching CI/CD misconfiguration before any content investment is made. A green deploy here means Phase 2 and 3 work lands on a proven foundation.
Output: Live site at https://prashantkhanchandani.github.io (dark background, placeholder text), green GitHub Actions workflow.
</objective>

<execution_context>
@/Users/prashantkhanchandani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prashantkhanchandani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Astro 5 project</name>
  <files>
    package.json
    astro.config.mjs
    tsconfig.json
    src/env.d.ts
    public/favicon.svg
    public/robots.txt
    src/pages/index.astro
  </files>
  <action>
    Scaffold a new Astro 5 project in the current directory (the repo root `/Users/prashantkhanchandani/Code/portfolio`). The directory is an existing empty git repo — scaffold into it directly, not into a subdirectory.

    Run:
    ```
    npm create astro@latest . -- --template empty --typescript strict --install --no-git
    ```

    The `--no-git` flag skips re-initializing git (the repo already exists). The `.` targets the current directory.

    After scaffolding, install additional dependencies:
    ```
    npx astro add sitemap --yes
    npm install -D sharp
    ```

    Then update `astro.config.mjs` to match this exact configuration (replace whatever the scaffold generated):
    ```javascript
    // astro.config.mjs
    import { defineConfig } from 'astro/config';
    import sitemap from '@astrojs/sitemap';

    export default defineConfig({
      site: 'https://prashantkhanchandani.github.io',
      // NO base: — this is a user root repo, serves from /
      output: 'static',
      trailingSlash: 'never',
      integrations: [sitemap()],
    });
    ```

    Critical: Do NOT set `base` to any value. This repo (`prashantkhanchandani.github.io`) is a GitHub user root repo — it serves at `/` with no sub-path. Setting `base: '/portfolio'` or any sub-path would break all asset and link paths.

    Replace `src/pages/index.astro` content with a minimal placeholder:
    ```astro
    ---
    // Phase 1 placeholder — replaced with real work index in Phase 2
    ---
    <!doctype html>
    <html lang="en">
      <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>Prashant Khanchandani</title>
        <style>
          html, body { background: #1e2030; color: #e8e8e8; font-family: system-ui, sans-serif; margin: 0; min-height: 100vh; display: grid; place-items: center; }
        </style>
      </head>
      <body>
        <p>Coming soon.</p>
      </body>
    </html>
    ```

    This inline style is intentional — it's only for this placeholder page. BaseLayout and global CSS arrive in Plan 02. The dark background prevents FOUC even before global.css exists.

    Do NOT install: `@astrojs/react`, `@astrojs/vue`, `@astrojs/tailwind`, `@astrojs/node`, `@astrojs/mdx`.
  </action>
  <verify>
    Run `npm run build` from the project root. Build must exit 0 with no errors. The `dist/` directory must exist after the build.
    Also run `npm run dev` briefly to confirm the dev server starts (Ctrl+C after confirming).
  </verify>
  <done>
    `npm run build` exits 0. `dist/index.html` exists. `astro.config.mjs` contains `site: 'https://prashantkhanchandani.github.io'` and does NOT contain `base:`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions deploy workflow</name>
  <files>
    .github/workflows/deploy.yml
  </files>
  <action>
    Create the directory `.github/workflows/` and write the following deploy workflow:

    ```yaml
    # .github/workflows/deploy.yml
    name: Deploy to GitHub Pages

    on:
      push:
        branches: [main]
      workflow_dispatch:

    permissions:
      contents: read
      pages: write
      id-token: write

    concurrency:
      group: "pages"
      cancel-in-progress: false

    jobs:
      build:
        runs-on: ubuntu-latest
        steps:
          - name: Checkout
            uses: actions/checkout@v4
          - name: Setup Pages
            uses: actions/configure-pages@v5
          - name: Install, build, and upload
            uses: withastro/action@v5

      deploy:
        needs: build
        runs-on: ubuntu-latest
        environment:
          name: github-pages
          url: ${{ steps.deployment.outputs.page_url }}
        steps:
          - name: Deploy to GitHub Pages
            id: deployment
            uses: actions/deploy-pages@v4
    ```

    Critical implementation notes:
    - Use `withastro/action@v5` (NOT v2, v3, or v4 — those are outdated)
    - Use `actions/configure-pages@v5` and `actions/deploy-pages@v4` (current versions)
    - `id-token: write` is REQUIRED at the top-level `permissions` block — `actions/deploy-pages` uses OIDC authentication and will fail with 403 without it
    - `workflow_dispatch` allows manual trigger from GitHub UI — useful for smoke testing
    - The `concurrency` block prevents two deploys from running simultaneously

    After writing the file, commit and push to main:
    ```
    git add .
    git commit -m "feat(01-foundation): scaffold Astro 5 with GitHub Pages deploy pipeline"
    git push origin main
    ```

    IMPORTANT: Before pushing, instruct the user to complete one manual step in GitHub repo Settings:
    Go to: Settings > Pages > Source > select "GitHub Actions" (not "Deploy from a branch").
    This must be set BEFORE the first push or the deploy step will fail silently (CI is green but the site never updates).

    After pushing, the GitHub Actions workflow will run automatically. Wait for it to complete and confirm it's green before proceeding.
  </action>
  <verify>
    After push to main:
    1. GitHub Actions tab shows a green checkmark on the "Deploy to GitHub Pages" workflow
    2. https://prashantkhanchandani.github.io loads and shows the dark placeholder page ("Coming soon." on dark background)
    3. No "Resource not accessible by integration" errors in the Actions log
  </verify>
  <done>
    Workflow runs green on push to main. https://prashantkhanchandani.github.io is live and shows dark placeholder content.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` exits 0 locally
2. `astro.config.mjs` has `site: 'https://prashantkhanchandani.github.io'`, no `base:`
3. `.github/workflows/deploy.yml` uses `withastro/action@v5` and has `id-token: write`
4. GitHub Actions workflow runs green on push to main
5. https://prashantkhanchandani.github.io is live with dark background
</verification>

<success_criteria>
- Pushing to main automatically deploys to https://prashantkhanchandani.github.io
- No manual deploy steps required after initial GitHub Pages source setting
- Build exits 0 locally
- Live site shows dark background (not white/unstyled)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` with what was built, key decisions made, and any issues encountered.
</output>
